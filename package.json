{
  "name": "ecommerce-test-automation-project",
  "version": "1.0.0",
  "private": true,
  "description": "E-Commerce Web Application with Playwright Test Automation",
  "scripts": {
    "start": "python -m http.server 8000",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
